# Memory.md — Project State (update at the END of every AI session)

> Purpose: lets a fresh AI session resume instantly without re-reading the codebase.
> The AI must update this file before ending any working session (see Rules.md).
> Keep it under ~150 lines — this is a summary, not a log. Prune stale detail.

## Current Status
- **Current phase:** Phase 7 (demo + README) code done and boot-verified; needs a human visual check before fully done. Phase 6 fully done (see prior entries).
- **Last session date:** 2026-08-09
- **Last thing completed:** Wrote `README.md` (real results table from every `metrics/*.json`, honest per-label precision@recall including where PRD's ≥80% recall target wasn't hit for rare labels, architecture diagrams, accurate quickstart, limitations section) and `demo/streamlit_app.py` (per `Design.md`'s exact layout/color spec — dark theme via `.streamlit/config.toml`, decision badge, per-label score bars with flag/block tick marks, 3 mild non-toxic preset buttons per Design.md's own "use mild examples" instruction, `st.cache_resource` loads the ONNX-INT8 model in-process so the demo runs standalone without the API server, same singleton pattern as Architecture.md Decision #1). Refactored `label_band()` out of `inference.py`'s `decide()` (was duplicated between `decide()` and the demo) — both now share it; added test coverage in `tests/test_inference.py`. Installed `streamlit` into `.venv_phase1` (last piece of the full `requirements.txt`). Ran the full test suite (17 tests: `test_inference.py` + `test_api.py`) — all pass. **Actually started the demo** (`streamlit run demo/streamlit_app.py --server.headless true`) and confirmed via curl it serves HTTP 200 with no startup traceback — but this only proves it boots, NOT that it looks right. **A human still needs to open `http://localhost:8501` in a real browser and visually check it against Design.md's mockup** (dark theme rendering, badge colors, bar tick marks, preset buttons working) before Phase 7 is actually done — that verification has NOT happened yet.
- **Real friction hit again this session (not a code bug, recurring from earlier):** this Mac's RAM ran critically low (~60-70MB free) multiple more times during Phase 7 work, stalling a pytest run for over an hour before one attempt was lost entirely to an unrelated session teardown and had to be re-run from scratch (eventually passed once memory eased, ~15 min). Same diagnostic (`vm_stat`/`sysctl vm.swapusage`) and same fix (close Chrome tabs/VS Code windows) as Decision noted last time — this is clearly a recurring condition on this specific machine, not a one-off. Expect it to keep happening in future sessions; check memory pressure early if anything hangs inexplicably.
- **Fresh-clone verification done (partial):** cloned straight from GitHub (`c3b7f59`) into a scratch dir, confirmed `data/`/`models/` correctly absent (git-ignored) and the folder tree matches, then ran the full test suite (51 tests) against that clean checkout using `.venv_phase1`'s already-installed deps — **all 51 passed in 34s**. This confirms the committed repo is self-consistent (no missing files, no `.gitignore` surprises, no hidden local-state dependencies) for the local dev-loop portion of the quickstart (clone → install → pytest). **NOT verified**: the Kaggle download + Colab GPU training/export portions of the quickstart — re-running those from scratch would take hours and wasn't attempted; that part of Phases.md's "runs from scratch" criterion remains unverified by an actual re-run, though the process itself (via `notebooks/02_train_colab.ipynb`) already succeeded multiple times this session for Phases 3-5.
- **Focal loss ablation implemented (not yet trained/validated):** researched literature on improving rare-label F1 (RAKSHAK 2026 multi-task DeBERTa paper, focal loss sources — see Decision #14) and implemented per-label binary focal loss (Lin et al. 2017) as a `training.loss: focal|bce_pos_weight` config toggle in `train.py`/`training.yaml`, so it can be compared against the original Phase 3 model rather than blindly replacing it. `compute_focal_alpha()` (pure numpy, tested) plus a `MultiLabelFocalLoss` nn.Module (tested against hand-derived values, exact match). All 8 `test_train.py` tests pass locally. **This has NOT been trained on Colab yet** — the code exists and is tested, but there's no new `metrics/distilbert.json`-equivalent to compare against the current 0.5513 macro F1. Added a Colab notebook section for this ablation run (separate checkpoint dir, doesn't overwrite the working Phase 3 model).
- **Unrelated open item, deprioritized mid-session:** the demo (`streamlit run demo/streamlit_app.py`) hit a real bug — `ModuleNotFoundError: No module named 'src'`, because `streamlit run` doesn't add the project root to `sys.path` the way `python -m` does (unlike every other entry point in this repo). Fixed with an explicit `sys.path.insert()` at the top of `demo/streamlit_app.py`. Also discovered and cleaned up an unrelated side effect: a newer `streamlit` version auto-installs ".agents/skills"/".claude/skills" symlinks on first `streamlit run` without asking — removed, not needed. **A second boot attempt after the fix ran 57+ minutes with no error but also no confirmed success** before being killed to refocus on the focal-loss work — this is unresolved and needs another real attempt+visual check, it is NOT the same "confirmed working" state as Phase 6's API server.
- **Next immediate task:** Two independent threads open: (1) get the focal-loss ablation actually trained on Colab and compare its `metrics/distilbert.json`-equivalent against the current 0.5513 macro F1 baseline before deciding whether to adopt it — Memory.md's numbers table should NOT be updated to reflect focal loss until that real run happens; (2) resolve the demo's still-unconfirmed state — restart it, this time watching for either success or a NEW error (the sys.path fix should have worked, but that specific successful run was never confirmed) — and get the user's visual check against Design.md.

## Phase Checklist
- [x] Phase 0 — Skeleton
- [x] Phase 1 — Data pipeline
- [x] Phase 2 — Baseline (`metrics/baseline.json`: macro F1 0.4449, macro PR-AUC 0.4775)
- [x] Phase 3 — DistilBERT fine-tune (`metrics/distilbert.json`: macro F1 0.5513, macro PR-AUC 0.5857, beats baseline; model saved to Drive via `notebooks/02_train_colab.ipynb`)
- [x] Phase 4 — Evaluation & thresholds (`configs/thresholds.json`, `metrics/pr_curves/`, `metrics/error_analysis.md` all present)
- [x] Phase 5 — ONNX + benchmark (`metrics/onnx_quantized.json`, `metrics/latency_benchmark.json` present; accuracy passes, **latency target (p95<50ms) NOT met on Colab's weak CPU** — see Decision #12)
- [x] Phase 6 — FastAPI serving (code + tests done AND live-verified: real curl of `/health` + `/moderate`, 24.77ms latency on this Mac)
- [ ] Phase 7 — Demo + README (code written, tests pass, server boot-verified via curl — still needs a human to visually check the demo in a browser)
- [ ] Phase 8 — Extras (optional)

## Key Numbers So Far (source of truth: metrics/*.json)
| Metric | Baseline | DistilBERT | ONNX-INT8 |
|--------|----------|------------|-----------|
| Macro F1 | 0.4449 | 0.5513 | 0.5547 |
| Macro PR-AUC | 0.4775 | 0.5857 | 0.5873 |
| Micro F1 | 0.5564 | 0.6392 | 0.6477 |
| Precision @ recall | (see per-label in metrics/baseline.json) | (see configs/thresholds.json) | not re-run per-label |
| p95 latency (CPU, bs=1, len=128, Colab 2-vCPU Xeon) | — | 192.3ms | 162.4ms (target <50ms — NOT met, see Decision #12) |

## Decisions Made (append-only; never silently reverse — see Rules.md)
1. 2026-07-20: Civil Comments toxicity binarized at ≥0.5 (per `configs/training.yaml: data.civil_comments_binarize_threshold`) — matches Jigsaw 2018 convention, documented in `preprocess.py` module docstring.
2. 2026-07-20: Civil Comments' `identity_attack` continuous score is mapped onto the Jigsaw 2018 `identity_hate` label (same underlying concept, different naming) — see `CIVIL_COMMENTS_LABEL_MAP` in `preprocess.py`.
3. 2026-07-20: Civil Comments rows with a missing subtype score are treated as 0 (non-toxic on that subtype), since only a subset of comments received full subtype annotation. Count of fills is logged, not silently dropped.
4. 2026-07-20: `download.py` fetches only `train.csv` per competition (not the full competition bundle) — cuts disk usage from ~2.3GB to ~844MB by skipping test sets/annotation files `preprocess.py` never reads.
5. 2026-07-20: `id` column is force-cast to `str` immediately after each CSV load. Real data exposed a bug: Jigsaw 2018 ids are hex strings (e.g. `dd47c73e9e4e11ad`) while Civil Comments ids are pure digits; pandas left the merged column as mixed-type `object`, and pyarrow's `to_parquet` guessed `int64` from the numeric majority then crashed on the first hex id. Fixed in both loaders.
6. 2026-07-20: Fixed two bugs in `tests/test_preprocess.py` found by a real (slow, 26-min) pytest run: (a) `test_merge_and_dedupe_...` passed raw whitespace-only text directly to `merge_and_dedupe`, but blank-filtering to `""` is `clean_text`'s job (always called upstream in the real pipeline) — test now runs `clean_text` first. (b) `test_rarest_label_...` gave `threat` 1 positive while every other label had 0, making `threat` the MOST common label, not the rarest — `rarest_label()` correctly returned `toxic` (tied at 0, first in label order); test now gives every other label more positives than `threat`. Neither was a bug in the actual pipeline code — the real end-to-end run already succeeded before these were found.
7. 2026-07-20: Baseline evaluator threshold fixed at 0.5 for reporting (`evaluate()` default) — this is NOT the same as the operational allow/flag/block thresholds, which come from Phase 4's PR-curve analysis and live in `configs/thresholds.json`. Don't conflate the two.
8. 2026-07-21: `pos_weight` in `train.py` is the literal, uncapped `n_negative/n_positive` per label, straight from Phases.md's spec ("pos_weight from training-set label frequencies") — NOT capped or sqrt-scaled. Given `severe_toxic`'s ~0.08% positive rate in train, expect a pos_weight around 1200+ for it. This could cause training instability (huge gradients from rare-label examples); if that happens in Colab, it's a known risk to investigate (try capping pos_weight, e.g. `np.minimum(pos_weight, some_max)`) rather than a surprise bug.
9. 2026-07-21: `train.py` defers ALL torch/transformers imports inside functions (never at module top-level) specifically so `compute_pos_weight` stays unit-testable on the lightweight local venv (`.venv_phase1`, no torch installed). Keep this pattern if train.py is edited — don't move torch imports to the top of the file.
10. 2026-07-21: Phase 4 `thresholds.py` design choices confirmed with the user (Phases.md was ambiguous on both): (a) `metrics/error_analysis.md` is 50 worst false positives + 50 worst false negatives **aggregated across all 6 labels** (ranked by model confidence), not 50-per-label — keeps the file readable. (b) The flag-band threshold is **per-label** (`flag_threshold` alongside `block_threshold` in `configs/thresholds.json`), same as the block threshold, since a single global cutoff would be meaningless across labels with very different score distributions (e.g. `severe_toxic` vs `toxic`). Same deferred-import pattern as Decision #9 applies here too: `matplotlib` and `transformers`/`torch` imports are inside functions, not at module top, so `select_thresholds`/`build_error_analysis` stay unit-testable on `.venv_phase1`.
11. 2026-07-22: `ORTQuantizer.quantize(save_dir=...)` names its output file `model_quantized.onnx`, NOT `model.onnx` — `ORTModelForSequenceClassification.from_pretrained()`'s default lookup only finds `model.onnx` and raised on the first real Colab run of `export_onnx.py`. Fixed by adding `QUANTIZED_FILE_NAME = "model_quantized.onnx"` in `export_onnx.py` and passing `file_name=QUANTIZED_FILE_NAME` explicitly wherever an INT8 directory is loaded (both `export_onnx.make_onnx_predict_fn` and `benchmark.build_onnx_predict_one`). If either file is touched again and INT8 loading breaks, check this first before assuming it's a new bug.
12. 2026-07-22: Phase 5's ONNX-INT8 p95 latency (162.4ms) does NOT meet Phases.md's <50ms acceptance target — measured on Colab's free-tier shared hardware (`Intel(R) Xeon(R) CPU @ 2.00GHz`, 2 vCPUs only). User explicitly chose to accept and document this honestly rather than re-benchmark on stronger hardware (Colab Pro or freeing local disk for this Mac's M3) — see the AskUserQuestion exchange this session. **Do not silently claim this target was met in the README or anywhere else** — the real number is 162.4ms p95, 3x over budget, and the reason (weak shared CPU, not a code/model problem) must be stated alongside it. If the user later wants a stronger number, re-run `notebooks/02_train_colab.ipynb` sections 7-8 on better hardware — the code itself needs no changes.
13. 2026-08-09: A single real request against the live Phase 6 server on this Mac (M3, single request not a full p50/p95/p99 benchmark) measured 24.77ms — under the <50ms target Colab's benchmark missed (Decision #12). This is real evidence the miss was hardware, not model/code, but it's ONE request on a different machine, not a proper apples-to-apples benchmark (no warmup/1000-run methodology like `benchmark.py` uses) — don't conflate the two numbers or present 24.77ms as if it were measured the same rigorous way. If the README wants a real local p50/p95/p99 to cite instead of/alongside the Colab numbers, re-run `src/evaluation/benchmark.py` locally (now feasible — torch/transformers/optimum/onnxruntime are installed in `.venv_phase1` as of this session) rather than reusing this one-off number.
14. 2026-08-09: Added `training.loss: "focal"` (default) as an alternative to the original `"bce_pos_weight"` (Phase 3), user-requested after a literature review specifically targeting Phase 4's rare-label recall collapse. Sources: [RAKSHAK (2026)](https://arxiv.org/html/2607.20450) — weighted auxiliary rare-label heads gave the biggest single F1 gain (+3.7) in that paper, bigger than their cross-domain data augmentation (+2.6); NOT implemented here (would mean an architecture change, bigger scope) but noted as the highest-ceiling follow-up if focal loss alone isn't enough. Focal loss itself (Lin et al. 2017, RetinaNet) — `gamma=2.0` default (literature-supported across alpha values; `gamma=5` consistently underperforms per multiple sources), `alpha` computed per-label via `compute_focal_alpha()` = `n_negative/n_total` (Lin et al.'s alpha_t convention) rather than reusing `pos_weight`'s unbounded ratio (Decision #8) — the two parameterizations are NOT interchangeable, don't conflate them. Kept switchable via config specifically so a focal-loss run can be A/B compared against the existing 0.5513-macro-F1 model rather than assumed better — **that comparison hasn't happened yet, don't update the Key Numbers table until it does**.

## Known Issues / Tech Debt
- **`pytest` (and likely other pandas/sklearn-heavy scripts) genuinely takes ~26 minutes to run on this host, period — this is NOT a tool/sandbox-specific artifact.** Earlier this session it looked like automated/backgrounded runs were uniquely "stalled" while the user's own Terminal was fast; that theory was WRONG. A full `pytest -q` run timed end-to-end in the user's own Terminal took exactly 1580s (26m 19s) for just 18 tests. The host is simply very slow for this kind of workload (likely disk + memory pressure — see disk space note below). **Practical implication: don't try to "fix" the slowness or diagnose it further — it's real, budget for it.** When running anything nontrivial (pytest, preprocess, baseline training), either wait it out patiently in the background, or ask the user to kick it off in their own terminal and check back later — don't assume 10-15 min of low CPU means something is broken.
- `pytest` full-suite pass IS now confirmed (18 tests, all pass after fixing 2 test-only bugs — see Decisions #6). Don't re-run it reflexively given the 26-min cost; only re-run when something specific needs verifying.
- Host disk space is tight (has fluctuated between ~400MB and ~6.6GB free over this project's sessions — user has been freeing space periodically). `data/raw/` now holds ~844MB (both `train.csv` files only). Check `df -h /` before any large local install or download.

## Environment Notes
- Training: Colab (GPU type: TBD). Inference benchmarks: (CPU model: TBD — record exactly, it goes in the README).
- Kaggle credentials: `~/.kaggle/kaggle.json`, built from username `aniketpatil0904` + a newer-style `KGAT_`-prefixed API token (Kaggle's classic `kaggle.json` schema still works with these tokens in the `key` field). Never committed. Both competition rules accepted by the user.
- `.venv_phase1` (gitignored) now has the FULL `requirements.txt` installed as of 2026-08-09 (torch 2.13.0, transformers 4.57.6, optimum 2.1.0, onnxruntime 1.28.0, plus fastapi/uvicorn/pydantic/httpx from Phase 6, plus the original pandas/numpy/pyarrow/scikit-learn/pytest/pyyaml/kaggle/tqdm) — it is no longer just the "lightweight" venv, real local model inference (ONNX serving, and in principle local training/export) is now possible on this machine. Disk was ~9-10GB free as of 2026-08-09.
- `models/onnx/int8/` now exists locally (git-ignored) — the real Phase 5 INT8 model + tokenizer, pulled from Drive. `models/onnx/fp32/` and the PyTorch `models/distilbert-finetuned/` were NOT pulled down (not needed once INT8 was confirmed as the model to serve) — don't assume they're present without checking.

## File Map Quick Reference (update if structure drifts from Architecture.md)
- Entry points: `src/data/preprocess.py`, `src/models/baseline.py`, `src/models/train.py`, `src/models/export_onnx.py`, `src/evaluation/benchmark.py`, `src/serving/app.py`, `demo/streamlit_app.py`
- Shared library (no CLI, imported by entry points): `src/evaluation/evaluate.py` (model-agnostic `evaluate()`, used by baseline now, will be reused unchanged for DistilBERT/ONNX)
- Configs: `configs/training.yaml` (now also has a `baseline:` section for TF-IDF/LogReg hyperparams), `configs/thresholds.json`, `configs/labels.json`
