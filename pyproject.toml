[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "modguard"
version = "0.1.0"
description = "Real-time toxic content moderation API (DistilBERT + ONNX Runtime)"
requires-python = ">=3.10"

[tool.pytest.ini_options]
testpaths = ["tests"]
